apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esoft-springboot-example.fullname" . }}-test-connection"
  labels:
    {{- include "esoft-springboot-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esoft-springboot-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
